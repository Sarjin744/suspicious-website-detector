services:
services:
  # 🌐 BACKEND SERVICE
  - type: web
    name: suspicious-backend
    env: node
    plan: free
    branch: main
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: PORT
        value: "5000"
      - key: MONGO_URI
        value: "mongodb+srv://admin:MyAtlasPass123@cluster0.k1v7p.mongodb.net/suspiciousDB?retryWrites=true&w=majority"
      - key: JWT_SECRET
        value: "J8dKp$7!sF9QzR@cX0TnV4bU2r"
      - key: NODE_ENV
        value: production
    autoDeploy: true

  # 💻 FRONTEND SERVICE (React)
  - type: static_site
    name: suspicious-frontend
    branch: main
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/build
    envVars:
      - key: REACT_APP_API_BASE_URL
        value: "https://suspicious-backend.onrender.com/api"
    autoDeploy: true
